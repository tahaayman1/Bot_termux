# 🤖 Telegram Userbot — مراقب الكلمات المفتاحية

بوت مستخدم (Userbot) لتيليجرام مبني بـ **Telethon**، يراقب كل الجروبات والقنوات اللي حسابك عضو فيها في الوقت الحقيقي، ويرسل تنبيه فوري عند وجود كلمة مفتاحية.

مصمم للعمل على **Termux** (أندرويد).

---

## ✨ المميزات

- 🔍 مراقبة **كل الجروبات والقنوات** تلقائياً (حتى ~1000 مجموعة)
- ⚡ **Event-driven** بدون polling — فوري ومن غير ضغط
- 🔤 دعم **كلمات عادية** + **Regex** (تعبيرات نمطية)
- 📋 نسخ التنبيه لل**حافظة** تلقائياً (Termux)
- 💾 حفظ الكلمات في **SQLite**
- 🔐 التحكم فقط من **صاحب الحساب**
- 🔄 إعادة اتصال تلقائية

---

## 📁 ملفات المشروع

```
Bot_termux/
├── main.py              ← الكود الأساسي
├── requirements.txt     ← المتطلبات
├── .env.example         ← نموذج المتغيرات
├── README.md            ← هذا الملف
└── scripts/
    └── run_termux.sh    ← سكربت التشغيل
```

---

## 🚀 دليل التشغيل (خطوة بخطوة)

### 1️⃣ أول مرة فقط (التثبيت والإعداد)

نفذ هذه الأوامر بالترتيب في Termux:

**1. تحديث النظام وتثبيت البرامج الأساسية:**
```bash
pkg update && pkg upgrade -y
pkg install python git termux-api -y
```

**2. تحميل البوت:**
```bash
git clone https://github.com/tahaayman1/Bot_termux.git
cd Bot_termux
```

**3. تثبيت المكاتب المطلوبة:**
```bash
pip install -r requirements.txt
```

**4. إعداد الملفات:**
```bash
cp .env.example .env
nano .env
```
*(عدّل `API_ID` و `API_HASH` ثم اضغط `Ctrl+X` ثم `Y` ثم `Enter` للحفظ)*

**5. التشغيل الأول (لتسجيل الدخول):**
```bash
python main.py
```
*(أدخل رقم هاتفك وكود التحقق الذي سيصلك على تيليجرام)*

---

### 2️⃣ كل مرة (التشغيل اليومي)

لو قفلت Termux وعايز تشغل البوت تاني:

```bash
cd Bot_termux
python main.py
```

---

### 3️⃣ التشغيل الدائم (في الخلفية 24/7)

عشان البوت يفضل شغال حتى لو خرجت من التطبيق:

**1. منع النظام من إيقاف التطبيق:**
```bash
termux-wake-lock
```

**2. تشغيل البوت في جلسة منفصلة:**
```bash
# تثبيت tmux لو مش عندك
pkg install tmux -y

# إنشاء جلسة جديدة
tmux new -s tgbot

# (داخل الجلسة) تشغيل البوت
python main.py
```

**3. الخروج من الجلسة (وترك البوت يعمل):**
- اضغط `Ctrl` + `B`
- اترك الأزرار ثم اضغط `D`

**4. للرجوع للبوت (عشان تشوف اللوج أو توقفه):**
```bash
tmux attach -t tgbot
```
*(لإيقافه نهائياً اضغط `Ctrl+C`)*

## 🔧 الأوامر

أرسل هذه الأوامر في **Saved Messages** (المحادثة مع نفسك):

| الأمر | الوصف |
|-------|-------|
| `+ كلمة` | إضافة كلمة مفتاحية (أو قائمة كلمات) |
| `- كلمة` | حذف كلمة مفتاحية (أو قائمة كلمات) |
| `#` | عرض قائمة الكلمات |
| `/on` | تفعيل المراقبة |
| `/off` | إيقاف المراقبة |
| `/status` | عرض حالة البوت |
| `/setlog` | تعيين القناة الحالية لاستلام التنبيهات |
| `/unsetlog` | إلغاء تعيين القناة (العودة لـ Saved Messages) |
| `/help` | عرض المساعدة |

### أمثلة:

**إضافة كلمات متعددة:**
```
+ كلمة1
كلمة2
كلمة3
```

**حذف كلمة:**
```
- كلمة_قديمة
```

**عرض القائمة:**
```
#
```

---

## 📢 شكل التنبيه

عند مطابقة كلمة مفتاحية، يصلك تنبيه في Saved Messages بالشكل التالي:

```
👥 المجموعة: QU | Nursing
👤 المرسل: أحمد
🆔 المعرف: ID: 5983892083
⏰ الوقت: 2026-02-08 14:52:04

📝 الرسالة الكاملة:
مطلوب ممرض خبرة 3 سنوات...

🔥 للتواصل السريع انسخ هذا الرابط:
tg://user?id=5983892083

🔗 رابط الرسالة:
https://t.me/group_username/12345

🎯 الكلمات المتطابقة: مطلوب, ممرض
```

---

## 🔄 التشغيل الدائم

### باستخدام tmux (مُوصى به)

```bash
pkg install tmux -y

# إنشاء جلسة جديدة
tmux new -s tgbot

# داخل الجلسة:
cd Bot_termux
python main.py

# للخروج من الجلسة (بدون إيقاف البوت):
# اضغط Ctrl+B ثم D

# للعودة للجلسة:
tmux attach -t tgbot

# لإنهاء الجلسة:
tmux kill-session -t tgbot
```

### منع Termux من النوم

```bash
# قبل تشغيل البوت:
termux-wake-lock

# لإلغاء القفل لاحقاً:
termux-wake-unlock
```

### تعطيل تحسين البطارية (مهم جداً!)

1. اذهب لإعدادات الجهاز ← **البطارية** ← **تحسين البطارية**
2. ابحث عن **Termux**
3. اختر **"عدم تحسين"** أو **"Don't optimize"**
4. في بعض الأجهزة (شاومي، هواوي، سامسونج):
   - تأكد أن Termux في **قائمة التطبيقات المحمية**
   - أوقف **التشغيل التلقائي** (Auto-start)
   - في MIUI: إعدادات ← تطبيقات ← Termux ← توفير الطاقة ← "بلا قيود"

---

## 🔧 استكشاف الأخطاء (Troubleshooting)

### البوت شغال بس مش بيبعت تنبيهات؟

1. **تأكد من تفعيل المراقبة:**
   ```
   /status    ← في Saved Messages
   ```
   لو ظاهر 🔴 متوقف، اكتب `/on`

2. **تفعيل وضع Debug (سجل تفصيلي):**
   ```bash
   nano .env
   ```
   أضف السطر ده:
   ```
   DEBUG_MODE=1
   ```
   احفظ (Ctrl+X, Y, Enter) وأعد تشغيل البوت

3. **راجع ملف اللوج:**
   ```bash
   tail -f bot.log
   ```
   هتشوف كل رسالة واصلة + الكلمات اللي بتتفحص

4. **جرّب كلمة بسيطة:**
   ```
   /add test
   ```
   ابعت "test" في أي جروب — المفروض يجيلك تنبيه فوري

---

## 📝 ملاحظات

- ملف الجلسة (`userbot_session.session`) يحتوي على بيانات تسجيل دخولك — **لا تشاركه مع أحد!**
- البوت يستخدم **حسابك الشخصي** (ليس بوت عادي)
- سجل الأحداث يُحفظ في `bot.log`
- الكلمات المفتاحية تُحفظ في `keywords.db`
- البوت يتعامل تلقائياً مع FloodWait وانقطاع الإنترنت

---

## ⚠️ الأمان

- **لا تشارك** ملف `.env` أو ملف الجلسة
- أضف `.env` و `*.session` لملف `.gitignore` قبل رفع المشروع
- فقط صاحب الحساب يقدر يتحكم بالبوت

---

## 📄 .gitignore المُوصى به

```gitignore
.env
*.session
keywords.db
bot.log
__pycache__/
venv/
```
